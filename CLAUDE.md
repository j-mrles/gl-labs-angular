# Project Rules

## Git & Deployment

- **Do NOT** run `git commit`, `git push`, or `firebase deploy` unless the user explicitly asks.
- Always wait for explicit instructions like "commit", "push", "deploy", etc. before taking these actions.
- Staging files with `git add` is fine as part of preparing a commit, but do not finalize the commit without approval.
